#include<stdio.h>
void main()
{
    int a,b,n;
    scanf("%d%d%d",n,a,b);
    int end=b,start=a;
    while(end>0)
    {
        for(int i=start;i<=end;i++)
        {
            int sum=0;
            sum+=i;
            if(sum==n)
            {
                
            }
        }
        end--;
    }
}